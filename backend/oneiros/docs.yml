version: '3'

services:
  docs:
    image: oneiros_local_docs
    container_name: oneiros_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./oneiros:/app/oneiros:z
    ports:
      - '9000:9000'
    command: /start-docs
